name: Build
on: 
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup GO
        uses: ./.github/actions/setup

      - name: Run Build
        run: make build

      - name: Run Tests
        run: make test
